[build]
  command = "npm run build"
  publish = "dist"

# SPA Routing Support (React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Asset caching (performance boost)
[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
